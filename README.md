**Run the following commands to run the frontend and backend:** <br><br>
cd to frontend and run **npm start** to run the react frontend <br>
cd to backend and run **python run.py** to run the backend
